const ADAPTER_MARKER = 'app-platform-pruned-route-ui-adapter'

/**
 * Provider connection setup is handled by a dedicated PageFly app-platform setup surface later.
 */
export default function ProviderConnectionForm(_props: Record<string, unknown>) {
  void ADAPTER_MARKER
  return null
}
