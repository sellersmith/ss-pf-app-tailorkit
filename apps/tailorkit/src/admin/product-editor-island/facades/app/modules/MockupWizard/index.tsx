const ADAPTER_MARKER = 'app-platform-pruned-mockup-wizard-adapter'

/**
 * MockupWizard pulls TailorKit image-processing/server utilities.
 * ProductEditor V0.1 keeps the mask wizard import path stable but does not execute the wizard.
 */
export default function MockupWizard(_props: Record<string, unknown>) {
  void ADAPTER_MARKER
  return null
}
