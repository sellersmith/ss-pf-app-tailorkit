import type { BaseOptionItem, BaseOptionSetProps } from '../types'

/**
 * Text-specific option item interface
 */
export interface TextOptionItem extends BaseOptionItem {
  // Any additional text-specific properties can be added here
}

/**
 * Text-specific option set props interface
 */
export interface TextOptionSetProps extends BaseOptionSetProps {
  // Any additional text-specific properties can be added here
}
