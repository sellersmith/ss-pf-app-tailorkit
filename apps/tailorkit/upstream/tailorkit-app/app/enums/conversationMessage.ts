export enum ConversationRole {
  ASSISTANT = 'assistant',
  USER = 'user',
  SYSTEM = 'system',
}

export enum FeedbackType {
  HELPFUL = 'helpful',
  UNHELPFUL = 'unhelpful',
}

export enum MessageClassification {
  TEXT = 'text',
  IMAGE = 'image',
}
