/**
 * A component that adds a gap to the end of the page.
 * @returns {React.ReactNode} A div with the height of the gap.
 */
export default function PageGap() {
  return <div style={{ height: '140px' }} />
}
